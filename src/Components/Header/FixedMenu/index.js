import React from 'react';
import './fixedMenu.css';
import Button from '../../Button';

const FixedMenu = () => {
  return (
    <div>
      <Logo />
      <Button></Button>
    </div>
  );
};

export default FixedMenu;
